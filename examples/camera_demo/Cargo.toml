[package]
name = "camera_demo"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow.workspace = true
sindri = { path = "../../crates/sindri" }

[package.metadata]
# Camera features demonstration

